# Bootstrap configuration - loads before application.properties
# This ensures .env file is loaded before Spring initializes beans

# Enable spring-dotenv to load .env file
spring.dotenv.enabled=true

# Log level during bootstrap
logging.level.root=INFO
